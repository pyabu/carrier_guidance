# robots.txt for careerguidance.me
# Last Updated: 2026-06-11
#
# Strategy:
#   - /login and /signup: crawlable, noindex via meta tag (avoids "indexed though blocked" GSC warning)
#   - /logout, /profile, /student-dashboard, /admin: blocked here (genuinely private, never need crawling)
#   - /api/: blocked (machine endpoints, not for humans or search)
#   - /static/uploads/: blocked (user-uploaded files, not for indexing)
#
# IMPORTANT: Do NOT block pages that already have a <meta name="robots" content="noindex"> tag.
# Doing so causes the "Indexed, though blocked by robots.txt" GSC warning.

User-agent: *
Allow: /

# Block strictly private/backend areas that should never be crawled.
# Auth pages (/login, /signup, /onboarding) are NOT blocked here —
# they use <meta name="robots" content="noindex, nofollow"> in HTML instead.
Disallow: /logout
Disallow: /profile
Disallow: /student-dashboard
Disallow: /admin/

# Block private API and upload endpoints only.
# DO NOT block /static/js/ or /static/css/ — Google needs these to render pages.
Disallow: /api/
Disallow: /static/uploads/
Disallow: /__pycache__/
Disallow: /.vercel/

# Block known aggressive SEO/bulk crawlers.
User-agent: AhrefsBot
Disallow: /

User-agent: SemrushBot
Disallow: /

User-agent: MJ12bot
Disallow: /

User-agent: DotBot
Disallow: /

User-agent: PetalBot
Disallow: /

User-agent: Scrapy
Disallow: /

# Bingbot rate limiting (Googlebot ignores Crawl-delay, so no Googlebot block needed).
User-agent: Bingbot
Crawl-delay: 1

User-agent: Slurp
Crawl-delay: 2

User-agent: DuckDuckBot
Crawl-delay: 2

Sitemap: https://careerguidance.me/sitemap.xml
